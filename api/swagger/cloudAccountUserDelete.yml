post:
  summary: Delete a Cloud Account User from the platform and cloud
  tags:
    - cloudAccount
  description: Used for removing a User's access to a Cloud Account
  security:
    - bearerAuth: []
  parameters:
    - name: x-sr-application
      in: header
      description: What application/version is calling the API
      required: true
      style: simple
      schema:
        type: string
        default: SR/Swagger
  requestBody:
    description: Cloud Account User payload
    required: true
    content:
      application/json:
        schema:
          oneOf:
            - $ref: '#/components/schemas/cloudAccountUserDelete_teamPayload'
            - $ref: '#/components/schemas/cloudAccountUserDelete_eventPayload'
  responses:
    '200':
      description: Successful operation
      headers:
        Content-Type:
          schema:
            type: string
          description: The MIME type of the payload
        Content-Length:
          schema:
            type: number
          description: Byte length of the payload
      content:
        application/json:
          schema:
            type: object
            properties:
              status_code:
                type: number
                example: 200
              user_uuid:
                type: string
                format: uuid
              cloud_account_user_id:
                type: string
    '403':
      description: Not authorized
    default:
      description: Default response for POST /cloudAccountUserDelete
components:
  schemas:
    cloudAccountUserDelete_eventPayload:
      type: object
      properties:
        cloud_account_user:
          type: object
          properties:
            team_member_uuid:
              type: string
              format: uuid
            event_uuid:
              type: string
              format: uuid
    cloudAccountUserDelete_teamPayload:
      type: object
      properties:
        cloud_account_user:
          type: object
          properties:
            team_member_uuid:
              type: string
              format: uuid
            team_uuid:
              type: string
              format: uuid
