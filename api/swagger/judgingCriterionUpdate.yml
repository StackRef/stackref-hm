post:
  summary: Updates a judging criterion on the platform
  tags:
    - judgingCriterion
  description: Updates a judging criterion item when supplied with its details. Returns `judging_criterion_uuid`.
  security:
    - bearerAuth: []
  parameters:
    - name: x-sr-application
      in: header
      description: What application/version is calling the API
      required: true
      style: simple
      schema:
        type: string
        default: SR/Swagger
  requestBody:
    description: Criterion update information
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            action:
              type: string
              default: update
            criterion:
              type: object
              properties:
                judging_criterion_uuid:
                  type: string
                  format: uuid
                organization_uuid:
                  type: string
                  format: uuid
                criterion_weight:
                  type: number
                category_id:
                  type: number
                criterion_details:
                  type: object
                  properties:
                    criterion_name:
                      type: string
                    criterion_summary:
                      type: string
                    criterion_description:
                      type: string
  responses:
    '403':
      description: Not authorized
    default:
      description: Default response for POST /judgingCriterionUpdate
