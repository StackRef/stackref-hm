This terraform is for initial setup of an account for a scheduled event. This includes:

* Tagging of the account with `stackref:organization_uuid` and `stackref:event_uuid`
* Deployment of `coach` instance with same tags and associated IAM profile
